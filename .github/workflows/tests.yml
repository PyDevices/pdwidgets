name: Unit tests

on:
  push:
    branches: [main]
    paths:
      - 'lib/**'
      - 'tests/**'
      - 'scripts/**'
  pull_request:
    paths:
      - 'lib/**'
      - 'tests/**'
      - 'scripts/**'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: PyDevices/micropython-hardware
          path: micropython-hardware
          sparse-checkout: |
            lib
          sparse-checkout-cone-mode: false
      - uses: actions/checkout@v4
        with:
          repository: PyDevices/pygraphics
          path: pygraphics
          sparse-checkout: |
            lib/pygraphics
          sparse-checkout-cone-mode: false
      - uses: actions/checkout@v4
        with:
          repository: PyDevices/palettes
          path: palettes
          sparse-checkout: |
            lib/palettes
          sparse-checkout-cone-mode: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install ruff
        run: pip install ruff
      - name: Ruff
        run: ruff check lib tests scripts
      - name: Unit tests
        env:
          PYTHONPATH: lib:tests/stubs:micropython-hardware/lib:pygraphics/lib:palettes/lib
        run: python -m unittest discover -s tests
